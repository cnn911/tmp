name: AI Generate & Run Tests

on:
  pull_request:
    types: [opened, reopened, ready_for_review, synchronize]
  issue_comment:
jobs:
  pr_agent_job:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
      contents: write
    name: Run pr agent on every pull request, respond to user comments
    steps:
      - name: PR Agent action step
        id: pragent
        uses: qodo-ai/pr-agent@main
        env:
          OPENROUTER_KEY: ${{ secrets.OPENROUTER_KEY }}
          GITHUB_TOKEN: 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
          PR_TEST.FILE: test2.py
          PR_TEST.TESTING_FRAMEWORK: pytest
